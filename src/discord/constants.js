const JOIN = 'join';
const LEAVE = 'leave';
const STOP = 'stop';
const PLAY = 'play';
const GET_QUEUE = 'queue';
const SKIP = 'skip';
const PAUSE = 'pause';
const RESUME = 'resume';

module.exports  = {
    JOIN: JOIN,
    LEAVE: LEAVE,
    STOP: STOP,
    PLAY: PLAY,
    GET_QUEUE: GET_QUEUE,
    SKIP: SKIP,
    PAUSE: PAUSE,
    RESUME: RESUME
};

