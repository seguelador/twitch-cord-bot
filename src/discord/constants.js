const JOIN = 'join';
const LEAVE = 'leave';
const STOP = 'stop';
const PLAY = 'play';
const PAUSE = 'pause';

module.exports  = {
    JOIN: JOIN,
    LEAVE: LEAVE,
    STOP: STOP,
    PLAY: PLAY,
    PAUSE: PAUSE
};

